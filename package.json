{
  "name": "satisfactory-automated-calculator",
  "version": "1.0.0",
  "description": "Tests locaux pour le code GAS S.A.T (sans déploiement)",
  "private": true,
  "scripts": {
    "test": "jest --testPathPatterns='tests/' --passWithNoTests",
    "test:watch": "jest --watch --testPathPatterns='tests/' --passWithNoTests",
    "lint:syntax": "node scripts/check-syntax.js",
    "parse-save": "node scripts/parse-save.js",
    "sat-calc": "node scripts/sat-calc.js"
  },
  "devDependencies": {
    "@etothepii/satisfactory-file-parser": "^4.0.1",
    "gas-mock-globals": "^2.0.3",
    "jest": "^30.3.0"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFiles": [
      "./tests/setup.js"
    ],
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  },
  "dependencies": {
    "clasp": "^1.0.0"
  }
}
